lightGallery(document.getElementById('HCISlides'),{
    mode: 'lg-fade',
    cssEasing : 'cubic-bezier(0.25, 0, 0.25, 1)'
});
lightGallery(document.getElementById('Appendix_BiWeekReport_1_Slides'),{
    mode: 'lg-fade',
    cssEasing : 'cubic-bezier(0.25, 0, 0.25, 1)'
});
lightGallery(document.getElementById('Appendix_BiWeekReport_2_Slides'),{
    mode: 'lg-fade',
    cssEasing : 'cubic-bezier(0.25, 0, 0.25, 1)'
});
lightGallery(document.getElementById('AchievementTable'),{
    mode: 'lg-fade',
    cssEasing : 'cubic-bezier(0.25, 0, 0.25, 1)'
});

lightGallery(document.getElementById('MosCoW'),{
    mode: 'lg-fade',
    cssEasing : 'cubic-bezier(0.25, 0, 0.25, 1)'
});

lightGallery(document.getElementById('MosCoW'),{
    mode: 'lg-fade',
    cssEasing : 'cubic-bezier(0.25, 0, 0.25, 1)'
});
lightGallery(document.getElementById('Appendix_DeploymentManual_Slides'),{
    mode: 'lg-fade',
    cssEasing : 'cubic-bezier(0.25, 0, 0.25, 1)'
});
lightGallery(document.getElementById('SystemArchitectureDiagram'),{
    mode: 'lg-fade',
    cssEasing : 'cubic-bezier(0.25, 0, 0.25, 1)'
});